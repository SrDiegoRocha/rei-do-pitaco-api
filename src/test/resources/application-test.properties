# Secret só para testes — nunca usado em produção (prod exige a env var JWT_SECRET).
# Tokens assinados com esta chave não valem em produção, então pode viver no repo.
reidopitaco.security.jwt.secret=test-only-secret-key-not-used-in-production-environments-1234567890
